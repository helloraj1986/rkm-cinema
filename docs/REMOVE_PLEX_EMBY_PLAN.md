# Remove Plex/Emby Support Plan — `refactor/remove-plex-emby`

**Status: EXECUTED 2026-09-11 — phases 1–5 all committed on this branch; awaiting the
user's RKM-HP eyeball + merge.** Branch `refactor/remove-plex-emby` was created from
`main` with this plan as its first commit, both prerequisites (`chore/retire-prod-stack`,
`feat/player-layout`) were merged to `main` first, and the branch was rebased onto that
`main` before phase 1 started. Result: **227 functional references / 38 files → 0** (see
§8 for the allow-list of what deliberately survives), **1,562 lines deleted outright**,
backend **502 → 482** tests (the deleted ones enumerated per commit), and the contract
**39 → 38 paths**.

**User decision it executes (2026-09-11):** *"i dont want use prod profile anymore as plex and
emby's role is taken by jellyfin"* — the deployment path is already gone
(`chore/retire-prod-stack` deleted `run-rkm-cinema.ps1` and made Jellyfin the only resolved
backend). This plan removes the **code** that is left behind: the Plex/Emby providers, the
Plex-only API surface, and the Plex-flavoured vocabulary in the domain and the contract.

---

## 1. Definition of done (all measurable)

1. **No *functional* Plex/Emby reference remains.** The proof is a grep with an explicit
   allow-list — not "zero hits", because some hits are legitimate and must survive:
   `grep -rin 'plex\|emby' backend --include=*.py | grep -v __pycache__` may return **only**:
   - **protocol names that are literally Emby's**: `provisioner/provision.py` and
     `scripts/probe_jellyfin_detail.py` send `X-Emby-Authorization` — that *is* Jellyfin's own
     auth header (Jellyfin forked Emby). Removing it breaks provisioning and probes.
   - **"Emby-derived API shape" notes** that explain Jellyfin itself (it shares `/Items`,
     `/Users/...` with Emby) — keep, but reword any that point at a *deleted file*
     (`services/library/jellyfin.py:4` says "Mirrors `services/library/emby.py`" → that file is
     going, so the sentence must change).
   - **"Plex-style" as a UI idiom** (`api/routes/jellyfin_detail.py:1,3`,
     `services/library/jellyfin.py:84,771`) — a preplay/detail *design* reference, not a server.
     **Do not "clean" these up.**
   - the legacy-value note on `MEDIA_SERVER` (see §3 rule 1).
   Everything *functional* must be gone: no provider class, no `/api/plex/*` route, no
   `PLEX_*`/`EMBY_*` setting, no `plexUrl`/`embyUrl`/`plexKey` field, no `has_emby()`, no
   `validate_required()` Plex requirement, no import of a deleted module.
2. `grep -rin 'plex\|emby' frontend/src` returns **zero** (no "Plex-style" idiom there today —
   verified 2026-09-11; if that changes, extend this criterion rather than renaming a design
   comment).
3. `docs/api/openapi.v1.json`: **39 → 38 paths** (`/api/plex/thumb` gone), and no
   `plexUrl` / `embyUrl` / `plexKey` anywhere in the snapshot; regenerated by
   `python backend/scripts/snapshot_openapi.py`, diff reviewed as *exactly* the removals.
4. `frontend/src/lib/api/types.ts` regenerated (`npm run generate:types`) — no Plex fields.
5. Backend suite green with the count **explained**, not just "passing": the deleted tests are
   enumerated in the commit message (baseline today: **502 passed**; ≥12 tests go outright —
   5 in `test_plex_ownership.py`, 7 in `test_watch_links.py` — plus the Plex/Emby halves of the
   mixed files, so expect **~485**).
6. `ruff`, `tsc --noEmit`, `vitest`, `npm run build` green.
7. **The api's startup log no longer cries wolf.** Today `Config.validate_required()`
   (`backend/config/settings.py:258–267`) treats `PLEX_TOKEN` as required and
   `api/main.py:69–72` logs `Missing required config: ['PLEX_TOKEN']` on every boot of a
   Jellyfin-only stack. Assert the startup log is clean (that warning is why someone would
   spend an evening hunting a phantom Plex token).
8. User eyeball on RKM-HP after an api + web rebuild: browse a library, open an item page,
   play something (direct + a transcoded episode), switch subtitles, open Settings (health
   cards), use "Watch on Jellyfin" from the watchlist, and request a title end-to-end.

---

## 2. Inventory — every touch point, with the action for each

Measured 2026-09-11 on `main`. Two instruments, because they answer different questions:

- **"mentions"** (below) = lines with the words plex/emby, a proxy for how surgical each edit is.
- **"functional reference"** = the §5-1b grep (`PLEX_URL|PLEX_TOKEN|EMBY_*|PLEX_SCAN_TTL|has_emby|plex_thumb|Plex|EmbyService|…`): **227 lines across 38 files today, target 0.**
  Re-run it before starting so you can show the number going to zero at the end.

The 227 functional lines are concentrated in the files below (`backend/services/plex.py` 30,
`config/settings.py` 22, `tests/test_library_providers.py` 18, `services/library/factory.py` 13,
`tests/test_resource_api.py` 12, `services/library/plex.py` 11, …).

### A. Delete outright (no live importer once the phases below are done)

| Path | Size | Note |
|---|---|---|
| `backend/services/plex.py` | 428 L, 91 hits | `PlexService`, `PlexMovie`, `PlexShow` |
| `backend/services/plex_check.py` | 183 L, 23 hits | only used by the two scripts in this table |
| `backend/services/emby.py` | 77 L, 13 hits | legacy `EmbyService`/`EmbyItem` façade over the provider |
| `backend/services/library/plex.py` | 190 L, 32 hits | imports `services.plex` — goes with it |
| `backend/services/library/emby.py` | 207 L, 30 hits | |
| `backend/api/routes/plex_thumb.py` | 30 L, 10 hits | route `GET /api/plex/thumb` (contract change → §4 Phase 3) |
| `backend/scripts/verify_plex.py` | 81 L, 20 hits | manual probe script |
| `backend/scripts/add_with_plex_check.py` | 112 L, 16 hits | |
| `backend/tests/test_plex_ownership.py` | 131 L, 21 hits | **5 tests collected** |
| `backend/tests/test_watch_links.py` | 123 L, 46 hits | **7 tests** — plex/emby deep-link cases only; re-add the Jellyfin equivalents in Phase 3 |
| **total** | **1,562 L** | counted 2026-09-11 |

### B. Strip the Plex/Emby branches (keep the files — Jellyfin lives in them)

| Path | Action |
|---|---|
| `backend/services/library/factory.py` | become jellyfin-only: drop the `plex`/`emby` branches, the `plex=` passthrough param and the `"plex" if plex is not None` rule added on 2026-09-11 |
| `backend/services/library/service.py` | drop the doc mentions of `"plex"`/`"emby"` provider names; the `LibraryService`/`LibraryProvider` abstraction itself **stays** (Jellyfin implements it — do not collapse it) |
| `backend/services/base.py` | delete `_plex_params()` (`X-Plex-Token`) |
| `backend/services/media_status.py:50`, `services/recommendations.py:73`, `services/reconciliation/reconciler.py:109–115` | comments only ("no parallel PlexService branch (§43)") — reword; the `plex=` wrapping seam goes with the factory |
| `backend/application/commands/request_media.py:66,74,189,194` | drop the `plex=` parameter through the chain |
| `backend/api/routes/suggest.py:461–467` | **live lazy import** `from services.plex import PlexService` → route it through `build_library_service()` like every other call site |
| `backend/scripts/add_watchlist_cron.py:50`, `backend/scripts/auto_complete.py:24,54` | swap `PlexLibraryProvider` for the factory / library service |
| `backend/services/__init__.py`, `backend/services/library/__init__.py` | remove the `PlexService`/`EmbyService`/`PlexLibraryProvider`/`EmbyLibraryProvider` imports + `__all__` entries |
| `backend/services/health.py` (107–114 + the `healths` map) | drop the `plex`/`emby` `ServiceHealth` entries so `/api/health` stops reporting them (the frontend Settings page then drops those cards automatically — `ConfigHealthView.tsx` renders whatever the endpoint returns plus its `SERVICES` list, which §2E also narrows) |
| `backend/domain/enums.py:36` | doc comment calls the media server facts "(Plex/*arr/qBittorrent)" — reword to the media server |
| `backend/services/library/jellyfin.py:4` | "Mirrors `services/library/emby.py`" points at a deleted file — reword to "shares Emby's API shape because Jellyfin is Emby-derived" |
| `backend/scripts/probe_jellyfin_detail.py:125` | **keep** `X-Emby-Authorization` (protocol header — §1) |
| `backend/core/exceptions.py` | remove `PlexUnavailableError`, `EmbyUnavailableError` (and any `ServiceTag`) + the assertions in `tests/test_error_handling.py` |
| `backend/infrastructure/database/db.py:62,83` | SQL comments say `provider TEXT -- 'plex' \| 'emby'`. **First check whether this module is live at all** (the app stores state as JSON via `WATCHLIST_DB_PATH`); if it is dead, delete it in a separate commit inside this branch and say so — do not silently change a persistence layer here |

### C. Rename the domain vocabulary (the "Plex is the source of truth" wording)

`backend/domain/status.py` (272 L, 24 hits) is the core of this: `plex_available`, `in_plex`,
`plex_url`, `plex_key`, `emby_available`, `emby_url`, and `WatchLinks.plex_url/plex_key/emby_url`;
`domain/state_machine.py` consumes it; `api/models.py:102–104,151,173,189–205` and
`api/routes/{library,status,config,suggest}.py` expose it; `services/library/watch_links.py`
serialises it (`{"plex": {...}, "emby": {...}, "jellyfin": {...}}`).

Target vocabulary (server-neutral, Jellyfin-backed): `in_library`, `library_available`,
`watch_url`, `server_item_id`, and `WatchLinks.watch_url`. Do the rename **mechanically in one
commit** so the diff is reviewable, and reword the module docstrings that call Plex "the source
of truth" to "the media server (Jellyfin)".

⚠ `WatchLinkResolver.resolve()` output is consumed by the frontend as a **map keyed by provider
name** — keep a `"jellyfin"` key in that map (the frontend already reads `watch.jellyfin`), and
only drop `plex`/`emby` alongside the frontend change (Phase 3).

### D. Contract (`/api/plex/thumb` + the response fields)

- Remove the route + its registration (`backend/api/main.py:8,52`) — **breaking change**.
- Drop `plexUrl`, `embyUrl`, `plexKey` from `api/models.py` (nothing in the React app reads
  `plexKey` today; the generated type at `frontend/src/lib/api/types.ts:1175` is the only trace).
- Write **`docs/adr/ADR-0004-remove-plex-emby-support.md`** (ADR-0001 freezes `/api` as
  immutable v1 and requires an explicit, recorded breaking change; ADR-0002/0003 are the
  precedent for numbering/format).
- Regenerate: `python backend/scripts/snapshot_openapi.py` (**expect 38 paths**) then
  `cd frontend && npm run generate:types`.
- Update the artwork-cache test + nginx in the same phase: `nginx/default.conf:43` caches
  `/api/(jellyfin/(poster|person|backdrop)|plex/thumb)$` → narrow to the jellyfin paths.
  `backend/tests/test_artwork_cache.py` (10 hits) asserts the plex side too.

### E. Frontend

| Path | Action |
|---|---|
| `frontend/src/features/library/lib.ts:21` | `posterUrl()`'s `item.thumb → /api/plex/thumb` fallback is dead once the route goes. Decide deliberately: either return `null` (item without a library id shows seeded art — the existing no-image path) or map the watchlist thumb to another proxy. There is a test at `lib.test.ts:224` pinning the old behaviour — replace it with the new expectation, don't just delete it |
| `frontend/src/features/watchlist/lib.ts:97–111,139–140,176–181` | drop `plexUrl`/`embyUrl` reads and the `"plex"`/`"emby"` branches of `availableWatchLinks()` (the `jellyfin` branch at 141/180 already exists) |
| `frontend/src/features/watchlist/lib.test.ts:124,138,141` | plex watch-link cases → jellyfin equivalents |
| `frontend/src/features/settings/ConfigHealthView.tsx:9–17` | `SERVICES` list drops `plex`/`emby` (health cards come from `/api/health`, which stops reporting them in Phase 2) |

### F. Config / env keys

*(line numbers are as of 2026-09-11 — grep the symbol, don't trust the offset)*

- `backend/config/settings.py`:
  - annotations: `PLEX_URL`/`PLEX_TOKEN` (65–66), `EMBY_URL`/`EMBY_API_KEY` (80–81),
    **`PLEX_BROWSER_URL` (86) and `EMBY_BROWSER_URL` (87)** — the deep-link hosts, easy to miss;
  - `_load()` assignments: 167–168, 191–192, 195–196, and `PLEX_SCAN_TTL` at 183–185;
  - `_EXTRA_ENV_KEYS` (21) — drop `"PLEX_SCAN_TTL"`;
  - `has_emby()` (278–279);
  - **`validate_required()` (258–267) requires `PLEX_TOKEN`** — drop that check. Its callers are
    `api/main.py:69` (startup warning), `scripts/auto_complete.py:40` and
    `scripts/daily_recommendations.py:54`. **Do not replace it with a hard Jellyfin requirement**:
    a fresh install legitimately has no `JELLYFIN_API_KEY` until the provisioner writes one, so a
    hard requirement would warn on every first boot. `/api/health` already reports the media
    server's configured/ok state, which is the right place for it;
  - `load_env()` (298–305) exposes `PLEX_URL`/`PLEX_TOKEN` for scripts — drop both entries.
  - Keep `MEDIA_HOST` — `JELLYFIN_URL` is derived from it.
- `render_config.py:72` passes `PLEX_URL`/`PLEX_TOKEN`/`EMBY_URL`/`EMBY_API_KEY` through to the
  api container → drop them.
- `.env.example` is already cleaned on `chore/retire-prod-stack` (Plex/Emby block removed).
- **Test surface to update (wider than the deletion list):**
  `test_config_env_passthrough.py:25` (`KNOWN_KEYS` — drop `PLEX_URL`, `PLEX_TOKEN`,
  `PLEX_SCAN_TTL`, `PLEX_BROWSER_URL`, `EMBY_URL`, `EMBY_API_KEY`, `EMBY_BROWSER_URL`
  **deliberately**; the test's own comment says a removed setting must be updated by hand, never
  silently), `test_api.py:83`, `test_artwork_cache.py:27,94`, `test_caching.py:281`,
  `test_download_status.py:101,174,200`, `test_error_handling.py:33,74`,
  `test_resource_api.py`, `test_status.py`, `test_reconciler.py`, `test_library_providers.py`.
- The provisioner writes only `JELLYFIN_*` + `MEDIA_SERVER: "jellyfin"` into
  `/shared/runtime.json` — **nothing to do**, and that runtime layer is why a provisioned stack
  already resolved to Jellyfin even before this work.

### G. Docs

- `docs/ARCHITECTURE.md` §7 (library providers), §8 (watch deep-links), §3 diagram
  ("External: Plex·Radarr·Sonarr…"), §5 module list, §9/§11 (state machine + API table rows
  `GET /api/plex/thumb`, `plexUrl`/`embyUrl`) → rewrite Jellyfin-only. This is the bulk of the
  doc work; §1/§12 were already corrected on `chore/retire-prod-stack`.
- `README.md` — the feature table's "Deep watch links" row (already Jellyfin-only on the ops
  branch) and anything else that implies multi-server support.
- **KEEP `docs/PLEX_UI_PLAN.md` and `docs/PLEX_VIEWS_PLAN.md`.** They are *UI design* plans
  (Plex-style preplay/views), not Plex-server code — `services/library/jellyfin.py:776` cites
  `PLEX_UI_PLAN.md` as the UI spec. Do not delete or rename them.
- Historical records stay as they are: `docs/PROGRESS.md` narratives, and the old plan docs'
  original port numbers and Plex references.

---

## 3. Traps and rules (each one has cost a session before)

1. **`MEDIA_SERVER` keeps accepting `plex`/`emby` as *values* forever.** Deleting the *keys*
   must not make an old `.env` blow up: `resolve_media_server()` already maps anything
   unrecognised to jellyfin, and `render_config.py` should keep *tolerating* the legacy value
   (warn, don't fail) so an un-updated `.env` on the Windows box still deploys. The deployment
   is what matters; the code simply never selects Plex again.
2. **Never eq-match or branch on a provider name that may be absent** — the repo's own
   "empty-id sentinel" lesson: `provider == "plex"` on a missing field is how a wrong record
   gets matched. After this work there is exactly one provider; say so in code rather than
   keeping a name lookup alive for symmetry.
3. **`X-Emby-Authorization` is Jellyfin's header.** Do not "clean up" that string in
   `provisioner/provision.py`.
4. **Do not collapse the `LibraryService`/`LibraryProvider` abstraction.** Jellyfin implements
   it, every call site goes through it, and §43 of the design spec ("no direct-route service
   branch") depends on it. Only the Plex/Emby *providers* go.
5. **`/api/plex/thumb` removal is a breaking contract change** → ADR + regenerated snapshot +
   regenerated typed client, in one commit. Do not hand-edit `docs/api/openapi.v1.json` or
   `frontend/src/lib/api/types.ts`.
6. **`db.py` may be dead code, and a persistence layer is not "code cleanup".** Verify usage
   first; if it is live, change only the comments here and open a separate plan.
7. **No data migration is needed** — verified: `data/rkm/watchlist.json` (the app's own store,
   25 KB) contains **zero** plex/emby references, and Jellyfin ids are what the app stores.
8. **The provisioner writes `MEDIA_SERVER: "jellyfin"` into `runtime.json`** and that layer
   overrides `.env` for the api — so the runtime behaviour has been Jellyfin-only for a while;
   this work makes the *code* say the same thing.

---

## 4. Phases (one commit each, gates green after every phase)

Gate per phase: `cd backend && python -m pytest -q && python -m ruff check .` plus, from
`frontend/`, `npx tsc --noEmit && npx vitest run && npm run build` once Phase 3 touches it.
Every commit message states the test-count delta and names the tests it deleted.

### Phase 1 — make the factory Jellyfin-only (no deletions)
`factory.py` returns a single Jellyfin provider, period; drop the `plex=` passthrough and its
rule; update `request_media.py`, `suggest.py:461`, `add_watchlist_cron.py`, `auto_complete.py`
to go through the factory. Update the tests that assert two providers
(`test_library_providers.py` 18 tests, `test_reconciler.py`, `test_resource_api.py`,
`test_download_status.py`'s legacy-plex test, `test_status.py`).
**Gate: suite green, no file deleted yet.**

### Phase 2 — delete the dead modules, scripts and their tests
Inventory §2A, plus §2B's `__init__` exports, `base.py::_plex_params()`, `core/exceptions.py`
and the `enum`/doc mentions. This is a net-negative-lines commit; the import graph is already
severed by Phase 1 (**verify each deletion has no importer** with a grep before removing it).
**Gate: suite green at the reduced count, ruff clean.**

### Phase 3 — the API contract + frontend + nginx + ADR
Inventory §2C (vocabulary rename) **then** §2D (route + fields + snapshot + ADR + typed client)
**then** §2E (frontend) **then** the nginx artwork location. Ideally this is 2–3 commits
(rename / contract / frontend) so each is reviewable; the vocabulary rename on its own is
mechanical and must not change any response *shape*.
**Gate: 38 paths, typed client regenerated, frontend gates green.**

### Phase 4 — config keys + env plumbing
Inventory §2F, including the `KNOWN_KEYS` update and the deliberate failure it causes first.
**Gate: suite green; a synthetic `MEDIA_SERVER=plex` `.env` still renders and still resolves
to Jellyfin (test it explicitly — that is the regression that would strand the Windows box).**

### Phase 5 — docs + close-out
Inventory §2G (ARCHITECTURE rewrite), the PROGRESS record, then the user's rebuild + eyeball
(api + web: `docker compose -p rkm-bundled up -d --build api web`), and merge.

---

## 5. Verification commands the executing session must run and show

```bash
# 1. the "nothing functional is left" proof. Every line this prints must be one of the
#    allow-listed categories in §1 — protocol header, Emby-derived-Api note, "Plex-style" UI
#    idiom, MEDIA_SERVER legacy note. Anything else is a missed reference.
grep -rin 'plex\|emby' backend --include=*.py | grep -v __pycache__
grep -rin 'plex\|emby' frontend/src | grep -v node_modules        # expect zero

# 1b. the functional proof (must end EMPTY; 227 lines / 38 files on main on 2026-09-11)
grep -rn "PLEX_URL\|PLEX_TOKEN\|EMBY_URL\|EMBY_API_KEY\|PLEX_BROWSER_URL\|EMBY_BROWSER_URL\|PLEX_SCAN_TTL\|has_emby\|plex_thumb\|PlexLibraryProvider\|EmbyLibraryProvider\|PlexService\|EmbyService" \
  backend render_config.py --include=*.py | grep -v __pycache__

# 2. the contract moved by exactly one path
python3 - <<'EOF'
import json; d = json.load(open('docs/api/openapi.v1.json'))
print(len(d['paths']), [p for p in d['paths'] if 'plex' in p or 'emby' in p])
print('plexUrl' in json.dumps(d), 'embyUrl' in json.dumps(d))
EOF

# 3. the suites
cd backend && python -m pytest -q && python -m ruff check .
cd ../frontend && npx tsc --noEmit && npx vitest run && npm run build

# 4. an old-style .env still deploys (the Windows box must not be stranded)
grep -n "MEDIA_SERVER" .env.example        # jellyfin documented; legacy values tolerated
```

---

## 6. Non-goals (do not do these here)

- Rewriting the `Plex-*` **UI design** plans, or any historical PROGRESS narrative.
- Collapsing `LibraryService`/`LibraryProvider`, or renaming the `services/library/` package.
- Deleting `db.py`, `nginx` routes, or any storage/schema change beyond the documented §2B
  comment fix.
- Touching `data/`, the Jellyfin volumes, or the provisioner beyond rule 3.
- Anything in `feat/player-layout`'s player code.

## 7. Sizing

**227 functional references across 38 files today**, of which **10 files / 1,562 lines are
deleted outright** (tests included) and the rest are branch/comment/name edits. Rough effort:
Phase 1 ≈ 1 sitting (mostly test triage across ~10 test files), Phase 2 ≈ short (deletions),
Phase 3 ≈ 1–2 sittings (it is the contract + frontend), Phase 4 ≈ short, Phase 5 ≈ docs.
The riskiest single step is the Phase 3 vocabulary rename — do it as its own commit and diff
review it, because it touches the domain, the API models and the frontend's watch-link map at
once. Second riskiest: `validate_required()` / `load_env()` in Phase 4, because both are read by
scripts as well as the api.

---

## 8. Corrections found while EXECUTING (2026-09-11)

The plan was a measurement, and three of its claims did not survive contact with the
code. Each is recorded here rather than silently edited, because the next session will
otherwise trust the original text.

1. **§1 criterion 2 was wrong — `frontend/src` DOES have "Plex-style" idioms (~15).**
   The criterion said the grep must reach zero because "no 'Plex-style' idiom [exists]
   there today". It does: the preplay/detail/player design comments and the
   `PLEX_UI_PLAN` / `PLEX_VIEWS_PLAN` doc names. The plan's own escape hatch applies —
   *extend the criterion rather than rename a design comment*. So the surviving
   frontend references are exactly: those design idioms, plus the retired key names
   that appear inside tests asserting their ABSENCE. Every *functional* frontend
   reference is gone (poster URL, watch links, Settings cards, config service map,
   user-facing copy).
2. **The contract move happened in phase 2, not phase 3.** Deleting
   `api/routes/plex_thumb.py` removes the path from the OpenAPI schema immediately, so
   39 → 38 landed with the deletion; the snapshot was regenerated in its own commit
   right after the phase-3 rename (an interim commit otherwise carried a stale
   artifact). The phase-3 contract commit then dropped the three response *fields* and
   regenerated again — which also absorbed PRE-EXISTING client drift (`types.ts` had
   not been regenerated since `/api/search/global` landed).
3. **`render_config.py` needed more than "tolerate the legacy value" — and writing the
   test first is what caught it.** `resolve_media_server()` returns a KNOWN retired
   value *verbatim* (it maps only UNKNOWN values to jellyfin), so a pass-through left
   `backend == "plex"` and **skipped the Jellyfin admin-password generation**, whose
   gate is `backend == "jellyfin"`. That is the "provisioner finished, yet every
   library is disabled" failure — it would have shipped silently behind a green
   deploy. The renderer now always renders `jellyfin` and warns about the raw value;
   the old `fail()` (which would strand a working box on an un-updated `.env`) is gone.

Also worth knowing (not plan errors, just discovered):

- Two *real* bugs fell out of the phase-3 rename: the reconciler hunted for a match
  whose `provider == "plex"` (so `server_item_id` was silently always empty on a
  Jellyfin stack), and `snapshot_to_status_result()` hardcoded the `plex` key of the
  provider-keyed watch map.
- `api/routes/config.py` reported the media server as DOWN whenever a retired
  `MEDIA_SERVER` value was still in `.env` (it compared the *resolved name*). It now
  reports whether the library provider is reachable.
- `nginx/default.conf` + `tools/verify_nginx_artwork_cache.py` narrowed to the
  jellyfin artwork paths, re-verified by EXECUTION (6/6 checks, `nginx -t` rc=0).
- **Unrelated doc drift left alone on purpose** (out of this plan's scope, but a lie
  the next session might trust): `docs/ARCHITECTURE.md` §10 still mentions
  `dashboard-data.json` built by `scripts/rebuild_dashboard.py`, and §11 still
  describes the removed `app.js`/`api.js` frontend.
